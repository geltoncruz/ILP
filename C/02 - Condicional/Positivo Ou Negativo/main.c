#include <stdio.h>
#include <stdlib.h>

int main() {

	int numero = 0;

	scanf("%d", &numero);

	if(numero < 0) {
		printf("Numero negativo");
	} else if (numero > 0 {
		printf("Numero Positivo");
	} else {
		printf("Numero Nulo.");
	}

	return 0;
}
